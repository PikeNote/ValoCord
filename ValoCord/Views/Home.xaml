<Page x:Class="ValoCord.Views.Home"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:viewModels="clr-namespace:ValoCord.ViewModels"
      xmlns:userControls="clr-namespace:ValoCord.UserControls"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:handlers="clr-namespace:ValoCord.Handlers"
      mc:Ignorable="d"
      Title="Home"
      d:DesignHeight="500"
      d:DesignWidth="1300"
      Background="Transparent">
    <Page.DataContext>
        <viewModels:HomeViewModel />
    </Page.DataContext>
    <Page.Resources>
    
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
        </Style>
        <Style x:Key="h2" TargetType="TextBlock">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="White"></Setter>
        </Style>
    </Page.Resources>
     <Grid Margin="20">
         <Grid.RowDefinitions>
             <RowDefinition Height="Auto"/>
             <RowDefinition Height="Auto"/>
             <RowDefinition Height="Auto"/>
             <RowDefinition Height="*"/>
         </Grid.RowDefinitions>
         <TextBlock Grid.Row="0" Text="Recent Games" Style="{StaticResource h2}"/>
        <Grid Grid.Row="1" MinHeight="100">
            <ItemsControl  ItemsSource="{Binding RecentVodCollection}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" x:Name="RecentVODsCollection">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel handlers:SpacingSetter.Spacing="10" Background="Transparent"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <userControls:MiniVODListItem />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <TextBlock Text="No recent VODs found."
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       FontSize="16">
                <TextBlock.Style>
                    <Style TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding HasItems, ElementName=RecentVODsCollection}" Value="False">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </Grid>
        <ui:ProgressRing Grid.Row="2" IsIndeterminate="True" BorderThickness="10" Width="100" Height="100" HorizontalAlignment="Center"
                               Visibility="{Binding IsLoading}" Margin="0 10 0 0 "/>
         <Grid Grid.Row="3" handlers:GridSpacer.ColumnSpacing="10">
             <Grid.ColumnDefinitions>
                 <ColumnDefinition Width="*"></ColumnDefinition>
                 <ColumnDefinition Width="Auto"></ColumnDefinition>
             </Grid.ColumnDefinitions>
             <Grid Grid.Column="0">
                 <Grid.RowDefinitions>
                     <RowDefinition Height="Auto"/>
                     <RowDefinition Height="*"/>
                 </Grid.RowDefinitions>
                 <TextBlock Grid.Row="0" Text="Latest Valorant News" Style="{StaticResource h2}"/>
                 <Canvas Grid.Row="1">
                     <ScrollViewer Height="{Binding Parent.ActualHeight, Mode=OneWay, RelativeSource={RelativeSource Self}}"
                                   Width="{Binding Parent.ActualWidth, Mode=OneWay, RelativeSource={RelativeSource Self}}">
                         <ItemsControl ItemsSource="{Binding RecentPatches}" >
                             <ItemsControl.ItemTemplate>
                                 <DataTemplate>
                                     <userControls:NewsItem/>
                                 </DataTemplate>
                             </ItemsControl.ItemTemplate>
                         </ItemsControl>
                     </ScrollViewer>
                 </Canvas>
             </Grid>
             <Grid Grid.Column="1" VerticalAlignment="Stretch">
                 <Grid.RowDefinitions>
                     <RowDefinition Height="Auto"/>
                     <RowDefinition Height="*"/>
                 </Grid.RowDefinitions>
                 <TextBlock Grid.Row="0" Text="Patch Notes" Style="{StaticResource h2}"/>
                 <Border Grid.Row="1" Background="#211F1F" CornerRadius="10" Width="415" VerticalAlignment="Stretch" Margin="5"> 
                     <TextBlock Margin="10">
                         <TextBlock.Text>
                             V1.0.0 - Initial Release
                             
                             =) TBD
                         </TextBlock.Text>
                     </TextBlock>
                 </Border>
             </Grid>
         </Grid>
    </Grid>
</Page>
