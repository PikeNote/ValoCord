<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:userControls="clr-namespace:ValoCord.UserControls"
             xmlns:viewModels="clr-namespace:ValoCord.ViewModels"
             xmlns:controls="clr-namespace:FluentAvalonia.UI.Controls;assembly=FluentAvalonia"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="ValoCord.Views.HomeView"
             x:DataType="viewModels:HomeViewModel">
    <UserControl.DataContext>
        <viewModels:HomeViewModel/>
    </UserControl.DataContext>
    <UserControl.Styles>
        <Style Selector="TextBlock.h2">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>
    </UserControl.Styles>
    <Grid Margin="20" RowDefinitions="Auto Auto Auto *">
        <TextBlock Grid.Row="0" Text="Recent Games" Classes="h2"/>
        <ItemsControl Grid.Row="1" ItemsSource="{Binding RecentVODCollection}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel ItemSpacing="10" Background="Transparent"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <userControls:MiniVODListItem />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <controls:ProgressRing Grid.Row="2" IsIndeterminate="True" BorderThickness="10" Width="100" Height="100" HorizontalAlignment="Center"
                               IsVisible="{Binding IsLoading}" Margin="0 10 0 0 "/>
         <Grid Grid.Row="3" ColumnDefinitions="* Auto" ColumnSpacing="10">
             <Grid Grid.Column="0" RowDefinitions="Auto *">
                 <TextBlock Grid.Row="0" Text="Latest Valorant News" Classes="h2"/>
                 <ScrollViewer Grid.Row="1">
                     <ItemsControl ItemsSource="{Binding RecentPatches}" >
                         <ItemsControl.ItemTemplate>
                             <DataTemplate>
                                 <userControls:NewsItem/>
                             </DataTemplate>
                         </ItemsControl.ItemTemplate>
                     </ItemsControl>
                 </ScrollViewer>
             </Grid>
             <Grid Grid.Column="1" VerticalAlignment="Stretch" RowDefinitions="Auto *">
                 <TextBlock Grid.Row="0" Text="Patch Notes" Classes="h2"/>
                 <Border Grid.Row="1" Background="#211F1F" CornerRadius="10" Width="415" VerticalAlignment="Stretch" Margin="5"> 
                     <TextBlock Margin="10">
                         <TextBlock.Text>
                             V1.0.0 - Initial Release
                             
                             =) TBD
                         </TextBlock.Text>
                     </TextBlock>
                 </Border>
             </Grid>
         </Grid>
    </Grid>
</UserControl>
