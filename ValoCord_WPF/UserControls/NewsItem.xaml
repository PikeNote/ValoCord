<UserControl x:Class="ValoCord_WPF.UserControls.NewsItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:ValoCord_WPF.UserControls"
             xmlns:data="clr-namespace:ValoCord_WPF.Data"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:system="clr-namespace:System;assembly=System.Runtime"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="1000"
             d:DataContext="{d:DesignInstance data:NewsData}">
    <UserControl.Resources>
        <Style x:Key="h3" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>
    </UserControl.Resources>
    <ui:Button Click="ButtonBase_OnClick" Background="Transparent" HorizontalAlignment="Stretch" Padding="5" CornerRadius="10" HorizontalContentAlignment="Stretch" BorderBrush="Transparent">
        <Grid Height="100" HorizontalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>
            <Border ClipToBounds="True" CornerRadius="10 0 0 10" x:Name="ImageBorder"/>
            <Border Grid.Column="1" ClipToBounds="True" CornerRadius="0 10 10 0" Background="#211F1F">
                <Grid Margin="10" >
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="{Binding title}" Style="{StaticResource h3}"/>
                    <TextBlock Grid.Row="1" Text="{Binding description.body}"/>
                    <TextBlock Grid.Row="2" Text="{Binding publishedAt}"/>
                </Grid>
            </Border>
        </Grid>
    </ui:Button>
</UserControl>
